// Escreva uma função que recebe 2 números e retorne o maior deles

function maior(x, y) {
  // if (num1 > num2)return num1;
  // return num2;
  return x>y?x:y;
}

const max2 = (x, y) => x>y?x:y;

console.log(max2(10, 50)); // 10